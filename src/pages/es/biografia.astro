---
import Layout from '../../layouts/Layout.astro';

const milestones = [
    { year: 1825, title: "Nacimiento en Badajoz", desc: "Hijo de músicos, inicia su formación bajo la tutela de su padre en la banda militar." },
    { year: 1844, title: "Llegada a Madrid", desc: "Se establece en la capital, destacando pronto como pianista virtuoso de salón." },
    { year: 1851, title: "Teatro de la Zarzuela", desc: "Funda, junto a Barbieri y Gaztambide, el teatro que cambiaría la historia lírica de España." },
    { year: 1870, title: "El Sitio de Zaragoza", desc: "Estrena su obra magna, consolidándose como referente del nacionalismo musical." },
    { year: 1877, title: "Fallecimiento", desc: "Muere inesperadamente en el Teatro Real de Madrid, dejando un vacío irremplazable." }
];
---

<Layout title="Biografía - Cristobal Oudrid" description="Cronología de la vida de Cristobal Oudrid." lang="es">
    <main class="py-24 px-6 max-w-4xl mx-auto">
        <header class="mb-24 text-center">
            <h1 class="text-5xl md:text-7xl font-serif font-bold text-accent-red mb-6">Biografía</h1>
            <p class="text-xl font-light tracking-wide text-ink/70">Una vida dedicada al arte y a España.</p>
        </header>

        <div class="relative border-l-2 border-accent-gold/30 ml-4 md:ml-0 md:pl-0 space-y-16">
            {milestones.map((item, index) => (
                <div 
                    class="relative pl-12 md:pl-0 md:grid md:grid-cols-12 md:gap-12 items-center group animate-fade-in-up opacity-0"
                    style={`animation-delay: ${index * 150}ms`}
                >
                    <!-- Dot -->
                    <div class="absolute left-[-5px] top-2 w-3 h-3 rounded-full bg-accent-gold md:left-auto md:right-[-6px] md:top-auto md:hidden"></div>
                    
                    <!-- Year (Left on Desktop) -->
                    <div class="hidden md:block md:col-span-5 text-right">
                        <span class="text-6xl font-serif font-bold text-accent-gold/20 group-hover:text-accent-red/20 transition-colors duration-500">
                            {item.year}
                        </span>
                    </div>

                    <!-- Divider Dot (Desktop) -->
                     <div class="hidden md:block col-span-2 relative h-full flex justify-center">
                         <div class="w-4 h-4 rounded-full bg-accent-gold border-4 border-paper z-10"></div>
                     </div>

                    <!-- Content (Right on Desktop) -->
                    <div class="md:col-span-5">
                        <span class="md:hidden text-4xl font-serif font-bold text-accent-gold/20 mb-2 block">{item.year}</span>
                        <h3 class="text-2xl font-serif font-bold text-ink mb-2">{item.title}</h3>
                        <p class="font-light text-ink/80 leading-relaxed">{item.desc}</p>
                    </div>
                </div>
            ))}
        </div>
    </main>
</Layout>
